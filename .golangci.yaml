version: "2"

linters:
  enable:
    - gocyclo
    - ineffassign
    - lll
    - misspell
    - staticcheck
    - revive
  settings:
    gocyclo:
      min-complexity: 15
  exclusions:
    presets:
      - std-error-handling

formatters:
  enable:
    - gofmt
